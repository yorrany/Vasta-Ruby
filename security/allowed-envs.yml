classifications:
  public:
    - NEXT_PUBLIC_API_URL
    - NEXT_PUBLIC_SUPABASE_URL
    - NEXT_PUBLIC_SUPABASE_ANON_KEY
    - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
    - NEXT_PUBLIC_STRIPE_PRICE_*
    - RAILS_ENV
    - NODE_ENV

  sensitive:
    - DATABASE_URL
    - REDIS_URL
    - STRIPE_SECRET_KEY
    - STRIPE_WEBHOOK_SECRET
    - SUPABASE_SERVICE_ROLE_KEY
    - MASTER_KEY

  critical:
    - MASTER_KEY
    - DATABASE_URL

audit:
  unknown_env_policy: "WARNING" # Warn if an env var is not classified
